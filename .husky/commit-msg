# Strip AI agent co-author trailers (Cursor injects these automatically).
if [ -n "$1" ] && [ -f "$1" ]; then
  sed -i '/^Co-authored-by: Cursor <cursoragent@cursor.com>$/d' "$1"
  sed -i '/^Co-authored-by:.*cursoragent@cursor\.com>$/d' "$1"
fi
